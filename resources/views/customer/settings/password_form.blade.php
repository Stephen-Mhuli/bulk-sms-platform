<div class="form-group">
    <label for="old_password">{{trans('customer.old_password')}}</label>
    <input type="password" name="old_password" class="form-control" id="old_password"
           placeholder="{{trans('customer.old_password')}}">
</div>

<div class="form-group">
    <label for="new_password">{{trans('customer.new_password')}}</label>
    <input type="password" name="new_password" class="form-control" id="new_password"
           placeholder="{{trans('customer.new_password')}}">
</div>

<div class="form-group">
    <label for="confirm_password">{{trans('customer.confirm_password')}}</label>
    <input type="password" name="new_password_confirmation" class="form-control" id="new_password_confirmation"
           placeholder="{{trans('customer.confirm_password')}}">
</div>
